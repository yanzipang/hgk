server:
  port: 4000
spring:
  application:
    name: atguigu-crowd-auth
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka